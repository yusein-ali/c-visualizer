; react-konva@16.9.0-1 declares peer react@16.9.x while the project is on
; react@16.14.0. Yarn 1 ignored peer conflicts; npm does not. react-konva is
; removed in Phase 8/9 of UPGRADE_PLAN.md (replaced by JointJS) — delete this
; file in the same commit that drops the dependency.
legacy-peer-deps=true
